# SoliGuard - 핵심 엔진/조치 계층은 추가 설치 없이 동작합니다(표준 라이브러리).
# 아래는 포맷 확장을 '활성화'하는 선택 의존성입니다. 없으면 해당 포맷은
# '검사불가'로 분리되고 나머지 스캔은 정상 진행됩니다.

# 조치 계층 - AES-256-GCM 암호화 격리 (권장)
cryptography>=42.0

# PDF 진단 리포트 생성 (권장)
reportlab>=4.1

# GUI (데스크톱 앱). 핵심 엔진/CLI는 PySide6 없이 동작.
PySide6>=6.6

# 디자인 파일 검사(디자이너 직무). XD는 표준 라이브러리로 동작.
psd-tools>=1.9     # PSD 텍스트 레이어·래스터 추출
requests>=2.31     # Figma REST API (옵트인 검사)

# 엑셀(.xlsx) 정밀 파싱 (없으면 표준 라이브러리 폴백 사용)
openpyxl>=3.1
# 구형 엑셀(.xls)
xlrd>=2.0
# 워드(.docx)
python-docx>=1.1
# PDF 텍스트 레이어
pdfplumber>=0.11
# 구형 한글(.hwp, OLE) — .hwpx 는 표준 라이브러리로 동작
olefile>=0.47

# 이미지/스캔 PDF OCR (로컬 처리). 별도로 Tesseract 엔진 + 한국어 데이터 필요.
pytesseract>=0.3.10
Pillow>=10.0
pdf2image>=1.17

# 인코딩 자동 감지 정확도 향상 (없으면 utf-8/cp949 순차 폴백)
chardet>=5.2
